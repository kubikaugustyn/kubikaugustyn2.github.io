import React from "react";
import {imgUrl, imgWidth} from "../count";

export default function Diamond() {
    return(
        <img alt="Diamond" src={imgUrl + "Diamond.png"} width={imgWidth} />
    )
}